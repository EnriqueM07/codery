<section
  id="inicio"
  class="grid gap-12 py-16 md:grid-cols-[minmax(0,1.5fr)_minmax(0,1fr)] md:py-24"
>
  <div class="flex flex-col justify-center">
    <p
      class="mb-4 text-xs font-semibold uppercase tracking-[0.28em] text-cyan-400"
    >
      Desarrollo de Software a la Medida · Integraciones con IA
    </p>

    <h1
      class="mb-5 text-4xl font-extrabold leading-tight md:text-5xl lg:text-6xl"
    >
      Transformamos tus ideas en
      <span
        class="bg-gradient-to-r from-indigo-400 via-violet-400 to-emerald-400 bg-clip-text text-transparent"
      >
        soluciones digitales reales con IA.
      </span>
    </h1>

    <p class="mb-4 max-w-xl text-lg text-slate-300">
      En <span class="font-semibold text-slate-100">Codery.mx</span> diseñamos y
      desarrollamos sistemas robustos y escalables:
      <span class="font-semibold">aplicaciones web, móviles, APIs</span> y
      paneles administrativos totalmente adaptados a la lógica única de tu
      negocio.
    </p>

    <p class="mb-8 max-w-xl text-sm text-slate-400">
      Además integramos <span class="font-semibold text-slate-100">tecnologías de Inteligencia Artificial</span>: 
      asistentes conversacionales, automatización de procesos, análisis de datos
      y generación de contenido para que tu software sea más inteligente y útil.
    </p>

    <div class="mb-10 flex flex-wrap gap-4">
      <a
        href="#contacto"
        class="btn-primary inline-flex items-center justify-center rounded-full bg-gradient-to-r from-indigo-600 to-violet-600 px-8 py-3 text-base font-semibold text-white shadow-xl shadow-indigo-500/50"
      >
        Iniciar un proyecto
      </a>
      <a
        href="#servicios"
        class="inline-flex items-center justify-center rounded-full border-2 border-slate-700 px-8 py-3 text-base font-semibold text-slate-200 transition duration-200 hover:border-violet-500 hover:text-violet-400"
      >
        Explorar servicios
      </a>
    </div>

    <!-- Datos clave -->
    <div class="mt-4 grid gap-4 text-sm text-slate-300 sm:grid-cols-3">
      <div
        class="rounded-xl border border-slate-800 bg-slate-900/60 p-4 transition duration-200 hover:border-indigo-500"
      >
        <i data-lucide="layers" class="mb-1 h-5 w-5 text-indigo-400"></i>
        <p
          class="text-[0.65rem] uppercase tracking-[0.18em] text-slate-400"
        >
          Expertise
        </p>
        <p class="mt-1 font-medium text-slate-200">
          Web · Mobile · APIs · IoT
        </p>
      </div>

      <div
        class="rounded-xl border border-slate-800 bg-slate-900/60 p-4 transition duration-200 hover:border-indigo-500"
      >
        <i data-lucide="sparkles" class="mb-1 h-5 w-5 text-emerald-400"></i>
        <p
          class="text-[0.65rem] uppercase tracking-[0.18em] text-slate-400"
        >
          IA &amp; Automatización
        </p>
        <p class="mt-1 font-medium text-slate-200">
          Chatbots, asistentes, análisis de datos, workflows inteligentes.
        </p>
      </div>

      <div
        class="rounded-xl border border-slate-800 bg-slate-900/60 p-4 transition duration-200 hover:border-indigo-500"
      >
        <i data-lucide="map-pin" class="mb-1 h-5 w-5 text-indigo-400"></i>
        <p
          class="text-[0.65rem] uppercase tracking-[0.18em] text-slate-400"
        >
          Operación
        </p>
        <p class="mt-1 font-medium text-slate-200">
          México y proyectos remotos globales
        </p>
      </div>
    </div>
  </div>

  <!-- Panel de código -->
  <div
    class="relative flex min-h-[300px] items-center justify-center overflow-hidden rounded-3xl border border-slate-800 bg-slate-900/80 p-6 shadow-2xl shadow-violet-900/40"
  >
    <div
      class="flex h-full w-full flex-col justify-start rounded-xl border border-slate-800/50 bg-slate-950 p-4"
    >
      <div class="mb-4 flex items-center space-x-2">
        <span class="h-3 w-3 rounded-full bg-red-500"></span>
        <span class="h-3 w-3 rounded-full bg-yellow-500"></span>
        <span class="h-3 w-3 rounded-full bg-green-500"></span>
        <p class="ml-4 text-xs text-slate-500">terminal/codery-ai.js</p>
      </div>

      <pre class="whitespace-pre-wrap text-xs lg:text-sm">
{`const generateIdea = async (brief) => {
  // Ejemplo: integración con un proveedor de IA
  const response = await fetch('/api/ai/idea', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ brief })
  });

  if (!response.ok) {
    throw new Error('No se pudo generar la propuesta.');
  }

  return response.json();
};`}
      </pre>
    </div>

    <div
      class="pointer-events-none absolute inset-0 rounded-3xl border-4 border-transparent"
      style="background: linear-gradient(135deg, rgba(99, 102, 241, 0.5) 0%, rgba(139, 92, 246, 0.05) 50%, rgba(0, 191, 255, 0.0) 100%) border-box;"
    ></div>
  </div>
</section>
